# 俄罗斯方块 - 概要设计

## 系统架构

```
┌──────────────┐     HTTP/JSON      ┌──────────────┐     SQLite     ┌──────────────┐
│   前端 SPA   │ ◄─────────────────► │  Flask API   │ ◄───────────► │  tetris.db   │
│  index.html  │     fetch / REST    │   app.py     │               │  SQLite 3    │
│  game.html   │                     │              │               │              │
└──────────────┘                     └──────────────┘               └──────────────┘
     │    │
     │    └─── tetris-core.js (游戏引擎)
     │         modes.js (游戏模式逻辑)
     │         auth.js (认证)
     │         ui.js (UI 工具)
     │
     └─── style.css (样式)
```

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML + CSS + JS（无框架） | SPA 式页面切换，路由用 URL hash |
| 游戏引擎 | tetris-core.js | SRS 旋转、7-bag、hold、幽灵方块、DAS/ARR |
| 模式管理 | modes.js | 策略模式，NormalMode / SpeedMode / RhythmMode / MutationMode |
| 后端 | Flask (Python 3) | RESTful API，JSON 通信 |
| 数据库 | SQLite 3 | 文件型数据库，tetris.db |
| 部署 | start.bat | 一键启动 Flask 服务器 |

## 模块划分

### 前端模块

| 模块 | 文件 | 职责 |
|------|------|------|
| 主页面 | index.html | 导航、模式选择、登录注册、设置 |
| 游戏页面 | game.html | Canvas-less DOM 渲染、键盘处理、引擎对接 |
| 核心引擎 | tetris-core.js | 棋盘状态、砖块生成/移动/旋转/锁定、消行 |
| 模式逻辑 | modes.js | 四种模式的 GameMode 子类，钩子方法 |
| 认证模块 | auth.js | 登录/注册/登出、本地 token 管理 |
| UI 工具 | ui.js | 消息提示、按键绑定交互 |

### 后端模块

| 模块 | 文件 | 职责 |
|------|------|------|
| API 入口 | app.py | Flask 路由，请求分发 |
| 数据库 | database.py | SQLite CRUD 封装，建表初始化 |

## 数据流

### 游戏启动流程

```
用户点击模式按钮
  → launchXxxMode()（index.html）
  → 构造 URL 参数（mode/type/target/bpm/mutation/preview）
  → 跳转 game.html?mode=xxx&...
  → 解析 URL_PARAMS 初始化 TetrisEngine
  → 创建对应 GameMode 实例
  → engine.start() → spawnPiece → requestAnimationFrame 循环
```

### 设置同步流程

```
用户修改按键/DAS/ARR/预览数量
  → saveAllSettings()（index.html）
  → POST /api/save_settings（Flask）
  → database.py 更新 users.key_settings / das_settings
  → 本地 localStorage 同步
```

### 变异模式数据流

```
用户勾选变异效果
  → launchMutationMode() 收集所有 :checked 值
  → URL: ?mode=mutation&mutation=no_gravity,double_size
  → game.html 创建 MutationMode(engine, {type: mutation})
  → engine.mutations = Set(['no_gravity', 'double_size'])
  → 若含 double_size → engine.enableDoubleSize()
  → game loop 中 mutation 钩子触发对应逻辑
```

## 目录结构

```
E:\t\
├── start.bat              # 一键启动脚本
├── docs/                  # 文档
│   ├── 需求设计.md
│   ├── ER图.md
│   ├── 概要设计.md
│   ├── 详细设计.md
│   └── 技术实现.md
├── backend/
│   ├── app.py             # Flask API 入口
│   ├── database.py        # SQLite 数据层
│   ├── requirements.txt   # Python 依赖
│   └── tetris.db          # SQLite 数据库
└── frontend/
    ├── index.html         # 主页面（模式选择/设置/账号）
    ├── game.html          # 游戏页面
    ├── css/
    │   └── style.css      # 全局样式
    └── js/
        ├── tetris-core.js # 游戏核心引擎
        ├── modes.js       # 游戏模式（Normal/Speed/Rhythm/Mutation）
        ├── auth.js        # 用户认证
        └── ui.js          # UI 工具函数
```
*（内容由AI生成，仅供参考）*
